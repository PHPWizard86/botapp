# Telegram Bot Token from BotFather
BOT_TOKEN=YOUR_BOT_TOKEN

# Telegram API URL (optional, defaults to official Telegram API)
# For Bale.ai, it might be something like: https://tapi.bale.ai/
BOT_API=

# Your Telegram User ID, used to identify the admin
ADMIN_ID=YOUR_TELEGRAM_ADMIN_ID

# Webhook Secret Token (if using webhooks for the bot, for security)
WEBHOOK_SECRET_TOKEN=YOUR_WEBHOOK_SECRET_TOKEN

# Base URL of your bot's web server (where index.php, sample.php, etc., are accessible)
# Example: https://yourdomain.com/botapp/
BASE_URL=https://yourdomain.com/

# Timezone for date functions
TIMEZONE=Asia/Tehran

# Image dimensions for cover processing
IMAGE_WIDTH=500
IMAGE_HEIGHT=500

# Base URL for the Instagram Downloader Node.js service
# Example: http://localhost:3000/ or http://your_instadown_service_domain/
INSTA_DOWNLOADER_BASE=http://localhost:3000/

# Optional: Proxy settings if your server needs a proxy to access external URLs
# PROXY_AUTH=username:password
# PROXY_DOMAIN=http://proxy.example.com:8080

# Optional: Paths for FFMPEG and FFPROBE if not in system PATH
# These are used by php-ffmpeg and potentially the instadown service if it uses them directly
# FFMPEG_PATH=/usr/bin/ffmpeg
# FFPROBE_PATH=/usr/bin/ffprobe

# --- API Specific Configuration ---
# A secret key for authenticating API requests from the Flutter app
API_ACCESS_KEY=SET_A_STRONG_RANDOM_API_KEY_HERE

# For initial development, you might want to restrict API access further
# Set to true to require a telegram_user_id matching ADMIN_ID for all API calls,
# or false to allow any valid API_ACCESS_KEY (user-specific data will still be scoped).
# This is mainly for early testing before full user-specific auth logic is in place.
API_ALLOW_ADMIN_ONLY_FOR_NOW=true
